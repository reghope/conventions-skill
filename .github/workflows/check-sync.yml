name: Check skill mirror
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify .claude/skills mirrors .github/skills
        run: diff -r .github/skills .claude/skills
